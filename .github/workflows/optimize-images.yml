name: Optimize images

on:
  pull_request:
    branches: [ master, main, release/** ]
    paths:
      - "**/*.png"

  push:
    branches: [ master, main, release/** ]
    paths:
      - "**/*.png"

jobs:
  optimize-images:
    uses: Shigure-Ruiseki/OK-Actions-Workflows/.github/workflows/optimize-images.yml@master
    secrets: inherit
